diff --git a/node_modules/react-native-youtube-iframe/index.d.ts b/node_modules/react-native-youtube-iframe/index.d.ts
index 5e00e80..e58697f 100644
--- a/node_modules/react-native-youtube-iframe/index.d.ts
+++ b/node_modules/react-native-youtube-iframe/index.d.ts
@@ -27,6 +27,8 @@ export interface YoutubeIframeRef {
   getPlaybackRate: () => Promise<number>;
   getAvailablePlaybackRates: () => Promise<number[]>;
   seekTo: (seconds: number, allowSeekAhead: boolean) => void;
+  setPlaybackQuality?: (quality: string) => void;
+  injectJavaScript?: (script: string) => void;
 }
 
 export interface InitialPlayerParams {
diff --git a/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js b/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js
index 0be2dad..d54f310 100644
--- a/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js
+++ b/node_modules/react-native-youtube-iframe/lib/commonjs/YoutubeIframe.js
@@ -50,11 +50,16 @@ const YoutubeIframe = (props, ref) => {
     if (!playerReady) {
       return;
     }
-    const message = JSON.stringify({
-      eventName,
-      meta
-    });
-    webViewRef.current.postMessage(message);
+    // PATCHED (quiet-feed): two stacked bugs made play/pause/mute no-ops.
+    // 1. react-native-webview's postMessage command is broken on Android under the New
+    //    Architecture — its document-dispatched MessageEvent never reaches the page.
+    //    Deliver via the injectJavaScript command instead (verified working — every
+    //    getter script already uses it), dispatched on `window`, where the page listens.
+    // 2. The hosted player page (iframe_v2.html) switches on the RAW string
+    //    ('playVideo' | 'pauseVideo' | 'muteVideo' | 'unMuteVideo'), not the JSON
+    //    envelope this function used to send — so send the bare eventName.
+    void meta;
+    webViewRef.current.injectJavaScript(`window.dispatchEvent(new MessageEvent('message', { data: ${JSON.stringify(eventName)} })); true;`);
   }, [playerReady]);
   (0, _react.useImperativeHandle)(ref, () => ({
     getVideoUrl: () => {
@@ -101,6 +106,12 @@ const YoutubeIframe = (props, ref) => {
     },
     seekTo: (seconds, allowSeekAhead) => {
       webViewRef.current.injectJavaScript(_PlayerScripts.PLAYER_FUNCTIONS.seekToScript(seconds, allowSeekAhead));
+    },
+    setPlaybackQuality: (quality) => {
+      webViewRef.current.injectJavaScript('try { if (player && player.setPlaybackQuality) player.setPlaybackQuality(' + JSON.stringify(quality) + '); if (player && player.setPlaybackQualityRange) player.setPlaybackQualityRange(' + JSON.stringify(quality) + ', ' + JSON.stringify(quality) + '); } catch(e){} true;');
+    },
+    injectJavaScript: (script) => {
+      webViewRef.current.injectJavaScript(script);
     }
   }), []);
   (0, _react.useEffect)(() => {
diff --git a/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js b/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js
index 4bbf487..e6fe8a3 100644
--- a/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js
+++ b/node_modules/react-native-youtube-iframe/src/YoutubeIframe.js
@@ -121,6 +121,14 @@ const YoutubeIframe = (props, ref) => {
           PLAYER_FUNCTIONS.seekToScript(seconds, allowSeekAhead),
         );
       },
+      setPlaybackQuality: (quality) => {
+        webViewRef.current.injectJavaScript(
+          `try { if (player && player.setPlaybackQuality) player.setPlaybackQuality(${JSON.stringify(quality)}); if (player && player.setPlaybackQualityRange) player.setPlaybackQualityRange(${JSON.stringify(quality)}, ${JSON.stringify(quality)}); } catch(e){} true;`
+        );
+      },
+      injectJavaScript: (script) => {
+        webViewRef.current.injectJavaScript(script);
+      },
     }),
     [],
   );
