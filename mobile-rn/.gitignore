node_modules/
.expo/
dist/
web-build/
# Native prebuild output (managed by EAS; regenerate with `expo prebuild`)
/android
/ios
*.log
.env
.env.*
!.env.example
*.jks
*.keystore
google-services.json

# Release signing. NEVER commit: keystore.json holds the keystore password, and the
# keystore itself is the only thing that can sign updates for an installed app —
# lose it and every user must uninstall/reinstall. Back it up somewhere safe.
/credentials
